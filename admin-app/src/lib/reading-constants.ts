// Highlight swatch palette (matches the design's 5 highlight colors).
// Kept in a plain module so it can be imported by both server actions and
// server components — a "use server" file may only export async functions.
export const HIGHLIGHT_COLORS = ["#FFD465", "#BF78F6", "#E98264", "#7FD1AE", "#8FB8E8"];
